# Prebuilt PVM guest pair for cube-gauge-* GitHub Releases.
#
# vmlinux-pvm-amd64 and cube_gauge-pvm-amd64.ext4 MUST come from the same
# kernel tree (md5 recorded in cube-gauge-metadata.json). The workflow uploads
# these files as-is so vermagic matches the vmlinux they were packed against.
